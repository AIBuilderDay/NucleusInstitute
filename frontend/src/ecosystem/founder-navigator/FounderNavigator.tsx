/**
 * STUB — replaced by the parallel session's real <FounderNavigator />.
 * Default-exports a section component, no required props.
 */
export function FounderNavigator() {
  return (
    <section
      className="card"
      style={{
        padding: "48px 24px",
        textAlign: "center",
        borderStyle: "dashed",
        borderColor: "var(--color-border)",
        background: "var(--pearl-100)",
        minHeight: 520,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <div className="tiny-caps">Founder's Navigator — coming soon</div>
    </section>
  );
}
